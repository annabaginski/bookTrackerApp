<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add it to the TBR </title>

    <!-- StyleSheet Link -->
    <link rel="stylesheet" href="css/styles.css" />
    <!-- Bootstrap Link -->
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />

    <!-- FontAwesome Link -->
    <script
      src="https://kit.fontawesome.com/215ecd0bb4.js"
      crossorigin="anonymous"
    ></script>

    <!-- LINK TO BOOTSTRAP -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
  </head>
  <body>
    <div id="heading">
      <h1 class="display-3">
        <i class="fa-solid fa-book"></i> My Reading Tracker
      </h1>
    </div>

    <div class="container">
      <% for (let i=0; i < books.length; i++){ %>
      <div class="box">
        <div class="item">
          <% if (books[i].completed == true){ %>
          <span class="completed"
            ><%= books[i].title %>, <%= books[i].author %>
          </span>
          <% }else { %>
          <span><%= books[i].title %>, <%= books[i].author %> </span>
          <% } %>
          <span class="fa fa-trash"></span>
        </div>
      </div>
      <% } %>

      <div class="form-box">
        <h2>Add a Book</h2>
        <form class="mb-3" method="post" action="">
          <input
            class="form-control"
            type="text"
            name="bookTitle"
            placeholder="Title"
          />
          <input
            class="form-control"
            type="text"
            name="bookAuthor"
            placeholder="Author"
          />
          <button class="btn btn-primary" type="submit">Add Book</button>
        </form>
      </div>
    </div>

    <div class="footer-padding"></div>
    <div class="footer">
      <p>Made by Anna Baginski</p>
    </div>

    <script src="js/main.js"></script>
  </body>
</html>
